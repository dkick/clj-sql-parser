(ns dkick.clj-sql-parser-test
  (:require [clojure.test :refer [deftest is testing]]
            [dkick.clj-sql-parser :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
